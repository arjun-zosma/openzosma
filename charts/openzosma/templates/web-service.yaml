{{- if .Values.web.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "openzosma.fullname" . }}-web
  labels:
    {{- include "openzosma.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.web.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openzosma.web.selectorLabels" . | nindent 4 }}
{{- end }}
